name: n8n_shopping_app
description: "A shopping app using n8n as a backend."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ">=3.9.2 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  genui: ^0.5.1
  json_schema_builder: ^0.1.3
  http: ^1.2.1
  url_launcher: ^6.3.1
  logging: ^1.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
